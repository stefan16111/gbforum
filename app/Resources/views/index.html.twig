{% extends 'base.html.twig' %}

{% block body %}
    <nav class="nav_bar" >
        <ul class="nav_links">
            <li><a href="#home">Główna</a></li>
            <li><a href="#news">Rekrutacja</a></li>
            <li><a href="#news">O Nas</a></li>
            <li><a href="#news">Regulamin</a></li>
            <li class="dropdown">
                <a href="javascript:void(0)" class="dropbtn">Bulidy</a>
                <div class="dropdown-content">
                    <a href="#">Link 1</a>
                    <a href="#">Link 2</a>
                    <a href="#">Link 3</a>
                </div>
            </li>
            <li style="float:right"><a href="{{ path('fos_user_security_login')}} ">Zaloguj</a></li>
        </ul>
    </nav>

    <div class="category_contaier">
        {% for category in categories %}
            <ul>  
                <li class="category_li"><a href="{{ path('show_category', {'id': category.id}) }}">{{ category.name }}</a>
                    {% for topic in category.topics %}
                        <ul class="topic_li">  
                            <li><a href="{{ path('allThreadsTopic', {'id': topic.id}) }}">{{ topic.name }}</a></li>
                        </ul> 
                    {% endfor %}
                </li>
            </ul> 
        {% endfor %}
    </div>
{% endblock %}